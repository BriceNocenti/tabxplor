# This file is intentionally empty and can be deleted (`git rm R/tab_logit.R`).
#
# Phase 12c renamed this module to R/tab_reg.R: the code was generalised from the binary-logit
# tab_logit()/multi_logit() into the unified regression-table engine tab_reg() (gaussian beta /
# binomial OR / poisson IRR). tab_logit()/multi_logit() remain, as thin binomial-family wrappers,
# in R/tab_reg.R.
