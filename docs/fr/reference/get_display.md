# get the "display" field of a `fmt` vector

get the "display" field of a `fmt` vector

## Usage

``` r
get_display(x)
```

## Arguments

- x:

  The formatted number in which you want to find data for "get"
  functions, to modify data for "set" functions.
