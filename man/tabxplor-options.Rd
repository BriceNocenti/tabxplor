% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabxplor-options.R
\name{tabxplor-options}
\alias{tabxplor-options}
\alias{tabxplor.options}
\title{tabxplor global options}
\description{
\code{tabxplor} reads its display, colour, statistics and export defaults from \code{options()}, all
prefixed \code{tabxplor.}. Set any of them for a session with \code{\link[=options]{options()}}, e.g.
\code{options(tabxplor.stars = TRUE)}, or once at the top of a script or \code{.Rmd}. The defaults are
established when the package loads (\code{.onLoad()}); most also have a per-call argument on the
relevant function, which always wins over the option.
}
\section{Display and printing}{

\describe{
\item{\code{tabxplor.print}}{\code{"console"} (default) or \code{"html"}: how a table auto-prints.
\code{"html"} renders the \code{\link[=tab_html]{tab_html()}} table (in the Viewer pane in RStudio/Positron, and
as a real html table in rmarkdown/Quarto documents) — recommended when you work in an
IDE with a Viewer. \code{"kable"} is an accepted synonym of \code{"html"} (the pre-2.0.0 name).}
\item{\code{tabxplor.stars}}{\code{FALSE} (default): whether cells show significance stars
(\code{*}/\verb{**}/\verb{***}). Off for \code{\link[=tab]{tab()}}, on for \code{\link[=tab_reg]{tab_reg()}}. Per-call \verb{stars =}.}
\item{\code{tabxplor.signif_levels}}{p-value cut-offs for the stars, default \code{c(0.10, 0.05, 0.01)}.}
\item{\code{tabxplor.signif_labels}}{the star labels, default \code{c("*", "**", "***")}.}
\item{\code{tabxplor.ci_print}}{\code{"ci"} (default) shows the \verb{[inf; sup]} interval; \code{"moe"} shows the
larger half-width (margin of error).}
\item{\code{tabxplor.var_names}}{which variable names the exporters annotate: \code{"both"} (default),
\code{"rows"}, \code{"cols"}, \code{"none"}. Per-call \verb{var_names =}.}
\item{\code{tabxplor.var_labels}}{\code{FALSE} (default): in \emph{exports} (markdown / html / Excel / plot),
show a variable's \emph{label} (the \code{haven}/\code{labelled} \code{label} attribute, if it has one) instead of
its name. Display only -- the table structure keeps canonical names, so name-based \code{select()}
and references still work; the console always shows names.}
\item{\code{tabxplor.cleannames}}{\code{FALSE} (default): clean up variable/level names in output. Also
strips a \code{"1-"}-style prefix from \code{labelled} value labels turned into factor levels.}
}
}

\section{Colours and theme}{

\describe{
\item{\code{tabxplor.color_breaks}}{the colour-break scales (a named list of \code{pct_diff},
\code{pct_ratio}, \code{odds_ratio}, \code{mean_diff}, \code{mean_ratio}, \code{contrib}, \code{zscore}). Set with \code{\link[=set_color_breaks]{set_color_breaks()}}.}
\item{\code{tabxplor.color_style_theme} (alias \code{tabxplor.console_theme})}{the \emph{console} palette theme,
\code{"light"} or \code{"dark"}; set by \code{\link[=set_color_palette]{set_color_palette()}} (which auto-detects the editor theme on load).
NOT the export theme (\code{tabxplor.theme} / \code{tabxplor.export_theme}).}
\item{\code{tabxplor.console_bold}}{whether to embolden the reference / total (and coloured) cells in the
\emph{console}, \code{TRUE} or \code{FALSE}. Auto-detected at load: \code{TRUE} in Positron and VS Code (which render
ANSI bold at a fixed glyph width), \code{FALSE} in RStudio and unknown consoles (there bold is drawn
wider and would break column alignment). Override it for your own front-end / font.}
\item{\code{tabxplor.theme} (alias \code{tabxplor.export_theme})}{the \emph{export} theme, \code{"light"} (default),
\code{"dark"}, \code{"print"} (the black-and-white publication palette; \code{"bw"} is a synonym) or \code{"auto"}
(follow the reader). \code{"auto"} needs a stylesheet, so only \code{tab_html()},
\code{\link[=tab_md]{tab_md()}} and \code{\link[=tab_css]{tab_css()}} honour it; static backends resolve it to \code{"light"}. \code{"print"} reaches
every backend, Excel included.}
\item{\code{tabxplor.color_style_type}}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} the console colour CHANNEL,
\code{"text"} or \code{"bg"}. Inert since 2.0.0: the channel is chosen per measure by
\code{color = c(text, background)} (see \code{\link[=tab]{tab()}}), so setting it emits a deprecation warning.}
\item{\code{tabxplor.print_rules}}{\code{TRUE} (default): every stylesheet \code{\link[=tab_css]{tab_css()}} emits also carries the
black-and-white publication palette inside an \verb{@media print} block, so a table rendered in colour
\strong{prints} (or saves to PDF) publication-ready with no further action. Set \code{FALSE} if your printer
is a colour one and the colours are the point.}
}
}

\section{Statistics and confidence intervals}{

\describe{
\item{\code{tabxplor.anova}}{which one-way ANOVA F is shown for mean columns: \code{"welch"} (default,
robust) or \code{"classic"} (pooled variance). Both are always stored in the \code{test} attribute.}
\item{\code{tabxplor.design_effect}}{\code{FALSE} by default: a weighted \code{\link[=tab]{tab()}} estimates the population
but bases every interval and test on the raw number of respondents, so they carry no design
effect --- and the table's footer says so. Set \code{TRUE} and the same intervals \strong{account for
the unequal weighting, exactly}: a weight column IS a survey design (the flat one, \code{ids = ~1}),
whose variance has a closed form in the per-cell \code{sum(w^2)} the aggregate already computes, so
the base becomes \code{n_eff = p(1-p) / Var_design(p)} in \strong{every weighted descriptive
confidence interval} --- factor proportions \emph{and} means (cell, difference, ratio and the
\code{color = "odds_ratio"} significance) --- and the whole-table tests (\code{test = TRUE}) become
\code{survey::svychisq} / a \code{svyglm} Wald F on that flat design. It reproduces \code{survey} to
the last digit, Kish's \verb{(sum w)^2 / sum(w^2)} being that same formula with each cell's own
\code{sum(w^2)} discarded. Being exact rather than a bound, it can make an interval \emph{narrower}
as well as wider. It is blind to \strong{clustering} and to \strong{calibration}, which the
weights do not record --- and those are not symmetric: missing the calibration and the strata
costs a few percent, in the safe direction, while missing the clusters of a face-to-face
household survey can leave an interval several times too short (see the Weights section of
\code{vignette("tabxplor")}). It needs the microdata weights, so \code{\link[=tab_counts]{tab_counts()}} on pre-aggregated
counts cannot apply it (such a table states the raw basis in its footer rather than claiming a
correction it does not have). \strong{Scope: \code{\link[=tab]{tab()}} and its leaves only.} \code{\link[=tab_reg]{tab_reg()}} never
reads it --- its crude \verb{empirical =} companions are always on the weighted basis, beside a model
column (\code{survey::svyglm}) that always was. For the full design effect --- strata, clusters,
\code{fpc}, calibration --- pass a \code{survey::svydesign} as \code{data}; the option is then not
consulted at all. The per-call \verb{design_effect =} argument of \code{\link[=tab]{tab()}}, \code{\link[=tab_num]{tab_num()}}
and \code{\link[=tab_plain]{tab_plain()}} overrides it (\code{NULL}, the default, means "use this option").}
\item{\code{tabxplor.conf_level}}{confidence level for the intervals and significance tests, default
\code{0.95}. The per-call \verb{conf_level =} argument of \code{\link[=tab]{tab()}}, \code{\link[=tab_num]{tab_num()}}, \code{\link[=tab_ci]{tab_ci()}} and \code{\link[=tab_reg]{tab_reg()}}
overrides it: since 2.0.0 each column records the level it was built at, so the colour
thresholds follow the argument and this option is the fallback for a column that never recorded
one (a hand-built \code{\link[=fmt]{fmt()}}, or a table from an older session).}
\item{\code{tabxplor.legend_style}}{the colour-legend style in exports (\code{\link[=tab_md]{tab_md()}}, \code{\link[=tab_kable]{tab_kable()}},
\code{\link[=tab_xl]{tab_xl()}}, \code{\link[=tab_plot]{tab_plot()}}): \code{"prose"} (default, full sentences) or \code{"terse"} (the compact
one-line form the console uses). The console itself is always terse.}
\item{\code{tabxplor.test_lines}}{how many crosstab test rows the exporters (\code{\link[=tab_md]{tab_md()}}, \code{\link[=tab_html]{tab_html()}},
\code{\link[=tab_xl]{tab_xl()}}) append: \code{"summary"} (default: p-value + effect size), \code{"all"} (+ the raw statistic),
\code{"stat"} (p-value + statistic), or \code{"pvalue"} (the single p-value row). The p-value row name states
the test used ("pvalue (Chi2, Welch F; survey-design)") and the effect-size row its measure ("Cramer's
V, eta2"). N is never added -- it is already shown by \code{add_n}. The console summary block always
shows N + p-value + effect size.}
\item{\code{tabxplor.spark}}{\code{TRUE} (default): in a \code{\link[=tab_reg]{tab_reg()}} table, a continuous predictor's row
label ends with a small curve showing the OBSERVED shape of its effect (ten bins of the outcome
against the predictor, on the model's scale, with no model in it) --- the eye-half of the
\code{Linearity} footer row. \code{"ascii"} uses a plain-text ladder for a console or a LaTeX font without
block characters; \code{FALSE} removes it. In HTML the glyphs become an inline SVG; a plot never
draws them (no graphics-device font has them).}
}
}

\section{HTML / \code{tab_html()} export}{

\describe{
\item{\code{tabxplor.tab_kable_css} (formerly \code{tabxplor.kable_css}, still accepted)}{\code{TRUE} (default):
inline the stylesheet with each \code{\link[=tab_html]{tab_html()}} / \code{\link[=tab_md]{tab_md()}} table (self-contained). Set \code{FALSE}
in a many-table document that emits \code{\link[=tab_css]{tab_css()}} once at the top.}
\item{\code{tabxplor.tab_kable_tooltips}}{\code{TRUE} (default): show the per-cell hover tooltips
(counts, confidence intervals, differences...) in html tables. Set \code{FALSE} once per
document when every table auto-prints and tooltips are unwanted. Per-call \verb{tooltips =}.}
\item{\code{tabxplor.kable_popover}}{\code{FALSE} (default): use click popovers instead of hover tooltips.}
\item{\code{tabxplor.tab_kable_num_font}}{the HTML/markdown number-font CSS stack. Monospace by
default so figures stay column-aligned (set a proportional stack to revert).}
\item{\code{tabxplor.output_kable}}{\code{FALSE} (default): make \code{\link[=tab]{tab()}} render its result with
\code{\link[=tab_html]{tab_html()}} before returning it — a convenience for \code{.Rmd}/\code{.qmd} documents. Since 2.0.0 it
only \emph{renders}: it no longer changes the shape of the built object (that is \code{output_list}).}
}
}

\section{Excel / \code{tab_xl()} export}{

\describe{
\item{\code{tabxplor.xl_font_text}}{text (labels/headers) font, default \code{"DejaVu Sans Condensed"}.}
\item{\code{tabxplor.xl_font_num}, \code{tabxplor.xl_font_num_stars}}{number font without / with stars,
defaults \code{"DejaVu Sans"} and \code{"Cascadia Mono"} (monospace, so stars align). xlsx records ONE
name (no fallback list), so set a font installed where the workbook is opened.}
\item{\code{tabxplor.xl_or_numeric}}{\code{FALSE} (default): keep odds ratios as numbers rather than
\code{1/x} text. Per-call \code{tab_xl(or_numeric =)}.}
}
}

\section{Plot, paths and language}{

\describe{
\item{\code{tabxplor.plot_num_font}}{the \code{\link[=tab_plot]{tab_plot()}} number font, applied only when the table shows
stars, default \code{"Cascadia Mono"} (\code{""} keeps the ggpubr default).}
\item{\code{tabxplor.export_dir}}{default directory for exported files (\code{NULL} = the working / typed
path).}
\item{\code{tabxplor.lang}}{the colour-legend language: \code{"auto"} (default, follows the R/OS locale),
\code{"en"} or \code{"fr"}. Per-call \verb{lang =}.}
}
}

\section{Parallel build}{

\describe{
\item{\code{tabxplor.parallel}}{\code{FALSE} (default): build the per-\code{row_var} tables of one \code{\link[=tab]{tab()}} call
on parallel CPU cores (needs the \code{mirai} package). \code{TRUE} = auto select number of cores,
integer = that many cores. Release the pool with \code{\link[=tab_parallel_stop]{tab_parallel_stop()}}.}
\item{\code{tabxplor.parallel_min}}{\code{2L} (default): the smallest \code{row_var} count worth dispatching
(fewer runs serially, since the setup would outweigh the gain).}
}
}

\section{jamovi live cache}{

\describe{
\item{\code{tabxplor.jmv_full_hash}}{\code{FALSE} (default): the jamovi module caches (Crosstables and
Regressions) fingerprint each data column cheaply by its class, factor levels and number of
missing values, so an unrelated edit does not invalidate every table. A same-shape value edit
(values changed but class, levels and NA-count unchanged) is therefore not detected and can serve
a stale cached result until the next structural change. Set to \code{TRUE} to hash the full column
values instead (exact, slightly slower) if you edit data in place and need every change caught.}
}
}

