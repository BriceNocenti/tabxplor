% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_classes.R
\name{tab_kable}
\alias{tab_kable}
\title{Print a tabxplor table in html}
\usage{
tab_kable(
  tabs,
  theme = NULL,
  color_type = lifecycle::deprecated(),
  html_24_bit = NULL,
  color = TRUE,
  tooltips = TRUE,
  popover = NULL,
  color_legend = TRUE,
  lang = NULL,
  caption = knitr::opts_current$get("tab.cap"),
  transpose = FALSE,
  var_names = NULL,
  html_font = NULL,
  get_data = FALSE,
  full_width = FALSE,
  wrap_rows = 35,
  wrap_cols = 15,
  whitespace_only = TRUE,
  engine = NULL,
  css = NULL,
  ...
)
}
\arguments{
\item{tabs}{A table made with \code{\link{tab}} or \code{\link{tab_many}},
or a \code{list} of tab with the same \code{col_vars} and no \code{tab_vars}.}

\item{theme}{By default (\code{"light"}) a white table with black text; \code{"dark"} for a black
table with white text; \code{"auto"} (opt-in) to follow whoever is \strong{reading} the table:
\itemize{
\item in a file or a knitted document, the reader's browser decides -- their operating system,
plus any dark-mode toggle of the host page (Quarto, Bootstrap 5.3, Tailwind);
\item printed to the \strong{Viewer}, your editor decides. Its webview reports the operating system
rather than the editor's colour theme, so the theme is resolved in R instead (RStudio's, or
Positron's, best-effort).
}
\code{"auto"} needs \code{engine = "html"} (kableExtra's themes are baked at render time); asking it of
the kableExtra engine renders light with a message. Defaults to \code{getOption("tabxplor.theme")},
i.e. \code{"light"} -- a dark table is always a deliberate choice.}

\item{color_type}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Inert since 1.4.0: the text channel always uses
the text palette. The colour CHANNEL is chosen by \code{color = c(text, background)} (see \code{\link{tab}}).}

\item{html_24_bit}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Inert since 1.4.0: exports are always
24-bit (the OKLCH palettes). Kept only so old calls do not error.}

\item{color}{Set to \code{FALSE} to render the table without colours (monochrome).}

\item{tooltips}{By default, html tooltips are used to display additional informations
at mouse hover. Set to \code{FALSE} to discard.}

\item{popover}{By default, takes \code{getOption("tabxplor.kable_popover")}. When
\code{FALSE}, html tooltips are of the base kind : they can't be used with floating table of
content in \pkg{rmarkdown} documents. Set to \code{TRUE} to use \pkg{kableExtra} html
popovers instead, which are compatible with floating toc. Remember
to enable the \code{popover} module by copying the following code into your document :
\code{<script>
$(document).ready(function(){
  $('[data-toggle="popover"]').popover();
});
</script>
}}

\item{color_legend}{Print colors legend below the table ?}

\item{lang}{Colour-legend language: \code{NULL} (auto from the R/OS locale, English fallback), \code{"en"} or \code{"fr"}.
You can then use a \code{css} chunk in rmarkdown to change popovers colors.}

\item{caption}{The table caption. For formatting, you need to use a \code{css}
with \verb{caption\{\}}in rmarkdown.}

\item{transpose}{Set to \code{TRUE} to transpose the table before export (rows become columns) --
the col-percentages-with-several-row-variables use case.}

\item{var_names}{Which variable names to write beside the table: \code{"both"} (the default),
\code{"rows"}, \code{"cols"} or \code{"none"}. The row-variable name is the leading column a table with
several \code{row_vars} uses to name each block (written once per block, vertically); the
column-variable names are the spanning row above their level columns. Level headers always
keep their name. Defaults to \code{getOption("tabxplor.var_names", "both")}.}

\item{html_font}{A string for HTML css font. By default, it uses
\code{'"DejaVu Sans", "Arial", arial, helvetica, sans-serif'}. Set another
default by setting \code{options("tabxplor.kable_html_font" = )}.}

\item{get_data}{Get the transformed data instead of the html table.}

\item{full_width}{A TRUE or FALSE variable controlling whether the HTML table
should have the preferable format for full_width. If not specified, a HTML
table will have full width by default but this option will be set to FALSE for
a LaTeX table.}

\item{wrap_rows}{By default, rownames are wrapped when larger than 30 characters.}

\item{wrap_cols}{By default, colnames are wrapped when larger than 12 characters.}

\item{whitespace_only}{Set to \code{FALSE} to wrap also on non whitespace characters.}

\item{engine}{The HTML render engine. \code{"html"} (default) is a dependency-free \verb{<table>} renderer:
faster, and every look is a CSS class you can restyle (see \code{\link[=tab_css]{tab_css()}}), which is what makes
\code{theme = "auto"} possible. \code{"kableExtra"} is the legacy engine (\pkg{kableExtra}); it bakes its own
theme, so it cannot follow the reader's colour scheme. Defaults to
\code{getOption("tabxplor.tab_kable_engine", "html")}.}

\item{css}{\code{engine = "html"} only: inline the stylesheet with the table, so the output is
self-contained (default, from \code{getOption("tabxplor.tab_kable_css")}). Set \code{FALSE} in a many-table
document that emits \code{\link{tab_css}} once at the top -- the stylesheet is table-independent,
so one copy styles every table. With \code{FALSE} and no \code{\link{tab_css}} call, tables render
uncoloured.}

\item{...}{Other arguments to pass to \code{\link[kableExtra:kable_styling]{kableExtra::kable_styling}}.}
}
\value{
A html table. Printing it opens it in the Viewer, on a page painted to match the table --
so a \code{theme = "dark"} table no longer sits in a white pane. Differences from totals, confidence
intervals, contribution to variance, and unweighted counts, are available in an html tooltip at
cells hover.
}
\description{
Print a tabxplor table in html
}
\examples{
\donttest{
tabs <- tab(forcats::gss_cat, race, marital, year, pct = "row", color = "diff")
tab_kable(tabs, theme = "light")
}
}
