% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-counts.R
\name{tab_counts}
\alias{tab_counts}
\title{Cross-table from already-aggregated counts ("from the middle")}
\usage{
tab_counts(
  data,
  row_var,
  col_var,
  tab_vars,
  counts,
  wt_counts,
  cols,
  col_name = "variable",
  base,
  input = c("counts", "pct"),
  pct = "no",
  color = "no",
  color_signif = "ignore",
  OR = "no",
  test = FALSE,
  na = "keep",
  cleannames = NULL,
  ref = "auto",
  ref2 = "first",
  comp = "tab",
  ci = "no",
  conf_level = getOption("tabxplor.conf_level", 0.95),
  stars = NULL,
  ci_method = NULL,
  totaltab = "line",
  totaltab_name = "Ensemble",
  tot = c("row", "col"),
  total_names = "Total",
  add_n = TRUE,
  add_pct = FALSE,
  common_totrow = FALSE,
  subtext = "",
  digits = 0,
  n_min = 0,
  display = NULL,
  color_breaks = NULL,
  spread_vars = character(),
  names_prefix = NULL,
  names_sort = FALSE,
  chi2 = lifecycle::deprecated()
)
}
\arguments{
\item{data}{A data frame of counts, or a \code{table} / \code{xtabs} / \code{matrix} object.}

\item{row_var}{The row variable (one level per line). For a \code{table} object it defaults to the
first dimension.}

\item{col_var}{The column variable (one column per level). For a \code{table} object it defaults to
the second dimension. Not used with \code{cols}.}

\item{tab_vars}{<\code{\link[tidyr:tidyr_tidy_select]{tidy-select}}> Tab variables: a subtable is made for
each combination of their levels.}

\item{counts}{The column holding the \strong{unweighted} count for each cell (long tidy shape).}

\item{wt_counts}{Optional column holding the \strong{weighted} count for each cell. Leave empty for an
unweighted table.}

\item{cols}{<\code{\link[tidyr:tidyr_tidy_select]{tidy-select}}> For a wide \code{data.frame}: the columns
holding the \code{col_var} levels.}

\item{col_name}{Name of the (synthesised) column variable when \code{cols} is used.}

\item{base}{For \code{input = "pct"}: the column holding each row's sample size N.}

\item{input}{\code{"counts"} (default) or \code{"pct"} (with \code{cols} and \code{base}: the level columns hold
frequencies, and counts are rebuilt from them and \code{base}).}

\item{pct, color, color_signif, OR, test, na, cleannames, ref, ref2, comp, ci, conf_level, stars, ci_method, totaltab, totaltab_name, tot, total_names, add_n, add_pct, common_totrow, subtext, digits, n_min, display, color_breaks, spread_vars, names_prefix, names_sort}{Same meaning as in \code{\link[=tab]{tab()}}. \code{color} accepts every form \code{\link[=tab]{tab()}} does (\code{FALSE} / \code{TRUE} /
a measure / \code{c(text, background)} / \code{list(pct =, mean =)}). Only \code{na = "keep"} / \code{"drop"} are
available (\code{"drop_all"} / \code{"common_base"} need the microdata). The \code{\link[=tab]{tab()}} arguments that pick or
collapse levels \emph{during the microdata prep} — which \code{tab_counts()} starts past — are not offered:
\code{levels = "first"} / \code{"auto"} (keeping a subset of levels), \code{other_if_less_than} / \code{other_level}
(lumping rare levels counts individual observations); build from microdata with \code{\link[=tab]{tab()}} for those.
Likewise the microdata-only / numeric-mean-only arguments: \code{wt} (use \code{wt_counts}); a survey
design as \code{data} (per-observation weights and structure a count table cannot carry -- it is
refused with a message); \code{design_effect} (same reason: no per-observation weights); \code{parallel};
\code{output_list}; \code{sup_cols}. The \code{mean_diff} / \code{mean_ratio} slots of \code{ci_method} are inert here
(a counts table has no numeric column).}

\item{chi2}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Renamed to \code{test} in 2.0.0 (see \code{\link[=tab]{tab()}}).}
}
\value{
A \code{tabxplor_tab} (or \code{tabxplor_grouped_tab} when \code{tab_vars} are provided).
}
\description{
\code{tab_counts()} builds the same color-coded cross-table as \code{\link[=tab]{tab()}}, but from data that is
\strong{already cross-tabulated} (a table of counts) rather than from microdata (one row per
individual). This is the common case when you start from a \code{dplyr::count()} result, a
contingency table, or a published table of counts or percentages. All the usual calculations
--- percentages, differences, confidence intervals, chi-squared, colors, totals --- are done on
the counts, and the result is identical to the table \code{\link[=tab]{tab()}} would build from the underlying
microdata.

It accepts four input shapes:
\itemize{
\item \strong{Long tidy counts} (the default): one row per \code{row_var} \eqn{\times} \code{col_var} (\eqn{\times}
\code{tab_vars}) combination, with the count in \code{counts} (and, weighted, the weighted count in
\code{wt_counts}).
\item \strong{A wide \code{data.frame}}: a label (\code{row_var}) column plus one column per \code{col_var} level ---
select those level columns with \code{cols} and name the column variable with \code{col_name}.
\item \strong{A \code{table} / \code{xtabs} / \code{matrix} object}: melted automatically; the row/column variables are
read from the dimnames (or set with \code{row_var} / \code{col_var}).
\item \strong{Frequencies + base N}: as the wide shape, plus \code{input = "pct"} and \code{base} (the column of
row sample sizes); the integer counts are rebuilt from the percentages and the base.
}

For weighted data, supply the real (unweighted) count in \code{counts} \strong{and} the weighted count in
\code{wt_counts}: estimates use the weighted counts while confidence intervals and tests use the real
unweighted sample size. When the counts are not real whole numbers (a base-less / weighted-only
input), confidence intervals and chi-squared are disabled with a message.
}
\examples{
# Long tidy counts (as from dplyr::count()) reproduce the microdata table :
counts <- dplyr::count(forcats::gss_cat, marital, race)
tab_counts(counts, marital, race, counts = n, pct = "row")
# tab(forcats::gss_cat, marital, race, pct = "row")   # identical

# A contingency table object :
tab_counts(table(forcats::gss_cat$marital, forcats::gss_cat$race), pct = "row")

# A wide data.frame of counts :
wide <- tidyr::pivot_wider(counts, names_from = race, values_from = n)
tab_counts(wide, row_var = marital, cols = c(Other, Black, White),
           col_name = "race", pct = "row")
}
