% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab-export.R
\name{tab_export}
\alias{tab_export}
\title{Export a tabxplor table to Excel, HTML, Markdown, or a plot}
\usage{
tab_export(
  x,
  format = c("html", "md", "xl", "plot"),
  path = NULL,
  theme = NULL,
  color_type = lifecycle::deprecated(),
  html_24_bit = NULL,
  color = TRUE,
  color_legend = TRUE,
  lang = NULL,
  transpose = FALSE,
  caption = NULL,
  var_names = NULL,
  ...
)
}
\arguments{
\item{x}{A table (or list of tables) made with \code{\link{tab}} / \code{\link{tab_many}}.}

\item{format}{One of \code{"html"} (the default), \code{"md"} (Markdown),
\code{"xl"} (Excel) or \code{"plot"} (a \code{ggplot}). The HTML backend engine
(home-built or kableExtra) is chosen with \code{engine =} (see \code{\link{tab_html}}).}

\item{path}{Optional output file. For \code{"xl"} it is the workbook path; for \code{"md"} and
\code{"html"} the rendered text is written to it; ignored for \code{"plot"}.}

\item{theme}{By default (\code{"light"}) a white table with black text; \code{"dark"} for the
inverse (colours follow the theme). \code{"auto"} follows the reader's colour scheme (their OS,
and any dark-mode toggle of the host page), which needs a stylesheet: it works for
\code{format = "kable"} with \code{engine = "html"} and for \code{"md"}, and resolves to
\code{"light"} for the static \code{"xl"} / \code{"plot"} backends and the kableExtra engine.
Defaults to \code{getOption("tabxplor.theme")}. See \code{\link{tab_css}}.}

\item{color_type}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Inert since 1.4.0: the text channel always uses
the text palette. The colour CHANNEL is chosen by \code{color = c(text, background)} (see \code{\link{tab}}).}

\item{html_24_bit}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#deprecated}{\figure{lifecycle-deprecated.svg}{options: alt='[Deprecated]'}}}{\strong{[Deprecated]}} Inert since 1.4.0 (exports are always 24-bit).}

\item{color}{Set to \code{FALSE} to render without colours (monochrome).}

\item{color_legend}{Print the colour legend with the subtext
(\code{"kable"}/\code{"md"}/\code{"xl"}/\code{"plot"}).}

\item{lang}{Legend language: \code{NULL} (auto from the R/OS locale, English fallback),
\code{"en"} or \code{"fr"}.}

\item{transpose}{Set to \code{TRUE} to transpose each table before export (rows become columns) --
the col-percentages-with-several-row-variables use case.}

\item{caption}{A single caption / title for the table.}

\item{var_names}{Which variable names to write beside the table: \code{"both"} (the default),
\code{"rows"}, \code{"cols"} or \code{"none"}. Defaults to \code{getOption("tabxplor.var_names", "both")}.
See \code{\link{tab_kable}}.}

\item{...}{Format-specific arguments passed to the underlying exporter.}
}
\value{
The value of the underlying exporter: an HTML/knitr object (\code{"html"}), a markdown
string (\code{"md"}), \code{x} invisibly with the Excel file written (\code{"xl"}), or a
\code{ggplot} (\code{"plot"}).
}
\description{
A single entry point that dispatches to the format-specific exporters
\code{\link{tab_html}} (HTML), \code{\link{tab_md}} (Markdown), \code{\link{tab_xl}} (Excel) and
\code{\link{tab_plot}} (a \code{ggplot}). The four functions share one set of display-option names
and defaults; \code{tab_export()} forwards them and passes any format-specific argument through
\code{...}.
}
\examples{
\donttest{
tabs <- tab(forcats::gss_cat, race, marital, pct = "row", color = "diff")
tab_export(tabs, "md")
}
}
