% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tablr_create.R
\name{tab_draw}
\alias{tab_draw}
\title{Draw tables from dataframe}
\usage{
tab_draw(
  wtable,
  result_text,
  result_num,
  row_var,
  col_var,
  tab_var,
  zone = c("base", "sup_cols", "sup_rows"),
  tot = c("row", "col"),
  totaltab = c("table", "line", "no"),
  keep_unused_levels = FALSE,
  subtext = "",
  reverse_row_col = FALSE
)
}
\arguments{
\item{wtable}{A dataframe with class \code{tab_df}.}

\item{result_text}{The variable containing the results to print with factors.
When empty, the \code{weighted_n} variable is taken
with no percentages, the \code{pct} with percentages.}

\item{result_num}{The variable containing the results to print with
numeric variables. When empty, \code{res} variable is taken.}

\item{row_var}{The row variable. When empty, takes the \code{var1} of
the \code{tab_df}.}

\item{col_var}{The column variable. When empty, takes the \code{var2} of
the \code{tab_df}.}

\item{tab_var}{The table variable. When empty, takes the \code{var3} of
the \code{tab_df}.}

\item{zone}{#The type of table to draw :
\itemize{
\item \code{"base"} is the standard crosstab
\item \code{"sup_cols"} is the table of supplementary columns
\item \code{"sup_rows"} is the table of supplementary rows
}}

\item{tot}{\code{"row"} add a total row, \code{"col"}
a total column, and \code{"all"} and \code{c("row", "col")} stand for both.
\code{"no"} removes all totals.}

\item{totaltab}{Only useful when \code{var3} is provided. \code{"table"}
means that a totaltable will be created. \code{"line"} means it will be
reduced to a single row. \code{"no"} means it will be totally removed.}

\item{keep_unused_levels}{Set to \code{TRUE} to keep empty levels of factors.}

\item{subtext}{A character vector to print legend rows under the
(list of) table(s) in \code{\link{tab_xl}}}

\item{reverse_row_col}{Transpose tables, with the row variable in colums
and the column variable in rows.}
}
\value{
A list of tables of class \code{\link{tab}},
or a \code{\link{single_tab}}.
}
\description{
Draw tables from \code{dataframe} of class \code{tab_df}, made
with \code{\link{tab_df}}.
}
\examples{
\dontrun{
forcats::gss_cat \%>\%
  tab_prepare(marital, relig, race, rare_to_other = TRUE) \%>\%
  tab_df(marital, relig, race, perc = "col") \%>\%
  tab_draw(totaltab = "line") \%>\%
  tab_xl(compact = TRUE)
  }
}
