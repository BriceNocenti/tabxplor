% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{preview_luminance_grid}
\alias{preview_luminance_grid}
\title{Preview luminance shades of one text/background pair in the Viewer}
\usage{
preview_luminance_grid(
  text_color,
  background_color,
  l_values = seq(0.35, 0.95, length.out = 7),
  chroma = c("fixed", "max"),
  sample_text = paste0(sample(0:100, 1L), "\%"),
  show_contrast = TRUE,
  table_bg = "#ffffff",
  dark_threshold = 0.5,
  font_size = "16px",
  swatch_padding = "12px 16px",
  browse = TRUE
)
}
\arguments{
\item{text_color}{Single hex string for the text colour.}

\item{background_color}{Single hex string for the background colour.}

\item{l_values}{Numeric oklch lightness values (0-1) for the shades.
Default seq(0.35, 0.95, length.out = 7).}

\item{chroma}{"fixed" (keep source chroma, capped to gamut) or "max"
(maximum in-gamut chroma per shade). Default "fixed".}

\item{sample_text, show_contrast, font_size, swatch_padding, browse}{See
\code{\link[=preview_colour_grid]{preview_colour_grid()}}.}

\item{table_bg}{Backdrop colour of the whole table/page, e.g. "#1a1a1a" to
preview dark mode. Default "#ffffff".}

\item{dark_threshold}{oklch lightness (0-1) of \code{table_bg} below which the
table switches to dark styling. Default 0.5.}
}
\value{
(Invisibly) the generated HTML as a single string.
}
\description{
For a single text colour and background colour, builds a grid of luminance
shades: rows vary the text colour's lightness, columns vary the background's
lightness. Every shade keeps its source oklch hue; chroma is either held at
the source value (capped to gamut) or pushed to the maximum available at that
lightness/hue. Rendered as a \code{\link[=tab_kable]{tab_kable()}}-style table (rounded
\code{cell_spec()} background tiles) and opened in the Positron Viewer.
}
\examples{
\dontrun{
preview_luminance_grid("#59c5bf", "#b9c653")                 # fixed chroma
preview_luminance_grid("#59c5bf", "#b9c653", chroma = "max") # most vivid
preview_luminance_grid("#59c5bf", "#b9c653", table_bg = "#1a1a1a")  # dark
}
}
\keyword{internal}
