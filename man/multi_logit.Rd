% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_logit.R
\name{multi_logit}
\alias{multi_logit}
\title{Compare several logistic-regression models (odds ratios side by side)}
\usage{
multi_logit(
  data,
  dependent,
  models,
  wt = NULL,
  inverse_two_level_factors = TRUE,
  conf_level = 0.95,
  method = c("wald", "profile"),
  color_signif = c("grey_non_signif", "ignore", "color_all_signif"),
  cleannames = NULL,
  subtext = ""
)
}
\arguments{
\item{data}{A data frame.}

\item{dependent}{Character. Name of the single binary dependent variable.}

\item{models}{A named list of character vectors; each element is one model's predictor set and
its name labels the column. Unnamed elements are labelled \code{model1}, \code{model2}, ...}

\item{wt}{Optional. Name of a weight column (character). Uses survey-weighted estimation.}

\item{inverse_two_level_factors}{Logical. If \code{TRUE} (default), models the FIRST level of a
2-level factor dependent (e.g. \code{"1-Married"} before \code{"2-Not married"}).}

\item{conf_level}{Confidence level for the odds-ratio intervals. Default \code{0.95}.}

\item{method}{How the interval and p-value are computed. \code{"wald"} (default) uses the Wald
interval \verb{exp(coef +/- z * se)} and the Wald z / t test: fast, matches standard software output
(R \code{summary()}, Stata, SPSS), and the only option for weighted models. \code{"profile"} uses the
profile-likelihood interval (\code{stats::confint()}, needs the \code{MASS} package) and the
likelihood-ratio test: more accurate for small samples or near-separation, but unweighted
models only (a weighted model falls back to Wald with a message).}

\item{color_signif}{How significance drives the colours (odds ratios only). \code{"grey_non_signif"}
(default) colours only odds ratios whose confidence interval excludes 1 and greys the rest;
\code{"ignore"} colours every odds ratio by magnitude; \code{"color_all_signif"} colours the significant
ones by their conservative interval bound.}

\item{cleannames}{Logical. If \code{TRUE}, strips numeric prefixes from factor levels for display
(e.g. \code{"1-Married"} -> \code{"Married"}). Uses \code{getOption("tabxplor.cleannames")} when \code{NULL}.}

\item{subtext}{Optional character. A note shown below the table.}
}
\value{
A \code{tabxplor_grouped_tab} (grouped by predictor), one odds-ratio column per model.
}
\description{
Fits several nested or competing models for ONE binary \code{dependent}, one per named predictor set
in \code{models}, and returns a \code{tabxplor} table with one odds-ratio column per model. Predictors
absent from a given model are left blank in that column, so the sensitivity of each odds ratio
to the model specification is read across a row. Same engine, fields and display as \code{\link[=tab_logit]{tab_logit()}}.
}
\examples{
data <- forcats::gss_cat |>
  dplyr::mutate(married = factor(dplyr::if_else(marital == "Married",
                                                "Married", "Not married")))
multi_logit(
  data, dependent = "married",
  models = list(demographic = c("race", "age"),
                full        = c("race", "age", "rincome"))
)

}
