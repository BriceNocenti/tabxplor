% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{preview_colour_grid}
\alias{preview_colour_grid}
\title{Preview every text x background colour combination in the Viewer}
\usage{
preview_colour_grid(
  text_colors,
  background_colors,
  sample_text = paste0(sample(0:100, 1L), "\%"),
  show_contrast = TRUE,
  font_size = "16px",
  swatch_padding = "12px 16px",
  browse = TRUE
)
}
\arguments{
\item{text_colors}{A (named) character vector of hex text colours. Names
become row labels; the hex value is used when unnamed.}

\item{background_colors}{A (named) character vector of hex background
colours, used as columns.}

\item{sample_text}{Text shown in each cell. Default: a random whole-number
percentage (e.g. "27\%"), one value per call.}

\item{show_contrast}{Logical; append the APCA lightness-contrast value (Lc)
of text-on-background to each cell, on the same line as sample_text.
Default TRUE.}

\item{font_size}{CSS font-size for the table. Default "16px" (tab_kable base).}

\item{swatch_padding}{CSS padding for each swatch cell. Default "12px 16px".}

\item{browse}{Logical; open the result in the Viewer. Default TRUE.}
}
\value{
(Invisibly) the generated HTML as a single string.
}
\description{
Builds a \code{\link[=tab_kable]{tab_kable()}}-style table with one row per text colour and
one column per background colour (cells are \code{kableExtra::cell_spec()} tiles,
the same rounded-background shape tab_kable() draws), then opens it in the
Positron Viewer pane.
}
\examples{
\dontrun{
text_colors <- c(plain = "#989898", pos3 = "#0baedb", pos5 = "#265aff")
background_colors <- c(plain = "#ffffff", pos3 = "#91b837", pos5 = "#05ae30")
preview_colour_grid(text_colors, background_colors)
}
}
\keyword{internal}
