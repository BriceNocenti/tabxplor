% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{forest_plot}
\alias{forest_plot}
\title{Forest plot of a tabxplor table}
\usage{
forest_plot(
  x,
  columns = NULL,
  what = c("auto", "effect", "level"),
  observed = c("auto", "band", "point", "ci", "none"),
  facet = NULL,
  color = TRUE,
  guide = c("gridlines", "bands", "none"),
  labels = c("none", "estimate"),
  stars = NULL,
  intercept = FALSE,
  totals = FALSE,
  size = NULL,
  theme = NULL,
  lang = NULL,
  caption = NULL,
  legend = TRUE,
  subtext = TRUE,
  return_data = FALSE,
  ...
)
}
\arguments{
\item{x}{A table made with \code{\link{tab}} or \code{\link{tab_reg}}, or a \code{list} of tab.
A list of tables sharing the same \code{col_vars} (and no \code{tab_vars}) is merged into one; any
other list --- several \code{row_vars} and/or \code{tab_vars} --- is rendered one table after
another, each keeping its own sub-tables.}

\item{columns}{Value columns to draw, by name. \code{NULL} (the default) draws the model columns of
a regression table and every value column of a cross-table.}

\item{what}{\code{"auto"} (the quantity the table's own interval is centred on), \code{"effect"}
(the contrast: difference, ratio or odds ratio) or \code{"level"} (the percentage or mean --
for a regression table this needs \code{effect = "marginal"}).}

\item{observed}{For a regression table with \code{empirical = TRUE}: \code{"auto"}, \code{"band"}
(the observed value with the margin of error of the gap), \code{"point"}, \code{"ci"} (the classic
two-interval figure) or \code{"none"}.}

\item{facet}{\code{NULL} for one panel per estimate column, \code{FALSE} for a single panel.}

\item{color}{Set to \code{FALSE} for a plain plot with no colour measure.}

\item{guide}{\code{"gridlines"} (the default), \code{"bands"} (shade the panel between the colour
breaks -- the teaching mode) or \code{"none"}.}

\item{labels}{Print the formatted estimate beside each point: \code{"none"}, \code{"estimate"}.}

\item{stars}{Show significance stars. \code{NULL} follows the table.}

\item{intercept}{Draw the regression \code{Constant} row.}

\item{totals}{Draw total rows and total columns.}

\item{size}{\code{NULL} for constant point size, or \code{"n"} to map the sample size.}

\item{theme}{\code{"light"}, \code{"dark"} or \code{"print"} (the black-and-white publication
palette). \code{NULL} follows \code{getOption("tabxplor.export_theme")}.}

\item{lang}{Colour-legend language: \code{NULL} (auto from the R/OS locale, English fallback),
\code{"en"} or \code{"fr"}.}

\item{caption}{A caption. \code{NULL} keeps the table's own.}

\item{legend}{Print the colour legend (as a guide, or in the caption when several ladders apply).}

\item{subtext}{Include the table's subtext and footer lines in the caption.}

\item{return_data}{Return the long estimate tibble instead of the plot.}

\item{...}{Unused.}
}
\value{
A \code{ggplot} (or a list of them), ready for \code{+ theme()} and \code{ggsave()}.
}
\description{
Draws every estimate of a table with its confidence interval, its significance and its colour --
for a cross-table from \code{\link{tab}} as much as for a regression table from
\code{\link{tab_reg}}. It \strong{reads the table and never re-fits anything}: every number and every
colour comes from the cell it was printed from, so the figure and the table cannot disagree.
(Its sibling \code{\link{reg_check_plots}} is the opposite: model checks \emph{always} re-fit, because
they are about residuals, which no table carries.)
}
\details{
\strong{What is drawn.} By default, whatever the table's own \code{ci =} produced: cell percentages with
their intervals for \code{ci = "cell"}, differences from the reference with a zero line for
\code{ci = "ref"}, odds ratios on a log axis for \code{display = "{or}"}, and the model's effects for a
\code{\link{tab_reg}} table. \code{what = "level"} / \code{"effect"} override it.

\strong{The gridlines are the table's colour ladder} (\code{\link{set_color_breaks}}), labelled with the
same glyphs the footer uses, and the colour of each point is the colour of its cell.

\strong{The observed comparison.} With \code{empirical = TRUE}, a regression estimate carries its crude
counterpart. \code{observed = "band"} (the default when the gap was testable) draws a bracket around
the observed value at plus-or-minus the margin of error \emph{of the difference}: the modelled point
falls outside it exactly when the gap test rejects. Two intervals must not be compared by overlap
when the estimators are correlated, which is why the crude interval is not drawn by default;
\code{observed = "ci"} restores that classic figure if you want it.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  t <- tab(forcats::gss_cat, race, marital, pct = "row", ci = "ref", color = TRUE)
  forest_plot(t)
}
}
\seealso{
\code{\link{reg_check_plots}} for the model checks, \code{\link{tab_plot}} to render the
table itself as an image.
}
