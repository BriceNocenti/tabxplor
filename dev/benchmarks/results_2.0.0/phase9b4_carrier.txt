Phase 9b-4 -- carrier to the tests boundary (2026-07-11)
========================================================

Change: fmt_unwrap()/fmt_wrap() carrier helpers (R/tab.R, next to fmt_materialize_col) +
a byte-identical no-op round-trip fmt_wrap(fmt_unwrap(tabs_text)) inserted in tab_transform()
right before the tab_apply_tests() seam. Foundation for 9b-5 (tests on plain fields).

Gate: devtools::test("d:/Statistiques/github/tabxplor") -> FAIL 0 | WARN 25 | SKIP 0 | PASS 1354
  (= pre-9b-4 1339 + 15 new test-carrier-parity.R). NO golden regeneration; all golden/parity
  suites green (test-golden / test-fmt-contract / test-color-golden / test-calculations /
  test-fuse-parity / test-num-fuse-parity / test-carve-parity / test-jmvtab-cache /
  test-parallel-parity). The 25 warnings are the pre-existing soft-deprecation warnings.

Bench: gss_cat, tab(5 row_vars x 3 col_vars, pct=row color=diff chi2=TRUE), merged, median of 7.
  "before" neutralizes the round-trip (both internals patched to identity -> composition == x).

  before (no round-trip) : 1.160 s
  after  (9b-4 no-op)    : 1.240 s
  overhead               : +0.080 s (+6.9%)

Reading: the +0.08 s is the TEMPORARY cost of materializing each row_var's joined factor table a
second time (leaf materializes for the record full_join, then the no-op wrap re-materializes it).
It is recovered in 9b-5: tab_apply_tests() consumes the carrier's plain fields (no per-step record
reconstruction, ~14% per §7.2) and the single fmt_wrap moves to end-of-build, so this extra
materialization disappears. 9b-4 is byte-identical foundation, "low payoff alone" as designed.
