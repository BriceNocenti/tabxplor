Phase 3b — Chi2/ANOVA onto the vectorised aggregate engine
Machine: Ryzen 7 5800X, data.table 8 threads. gss_cat (21,483 rows).
Scenario: tab_many, 3 row_vars x 3 factor col_vars (no name overlap) = 9 tables.
Warm system.time, median of 5.

                         chi2=TRUE   chi2=FALSE   chi2 SHARE
  BEFORE (pre-3b, HEAD)    3.070 s     0.470 s     2.600 s (85%)
  AFTER  (Phase 3b)        1.480 s     0.450 s     1.030 s (70%)

  => chi2 computation ~2.5x faster (2.60 -> 1.03 s); whole call ~2.1x faster.
     Remaining chi2 share = fmt-cell extraction (get_n per column -> long DT) +
     the tab_pvalue_lines row baking; the vectorised agg_chi2 core is a small part.

Numeric ANOVA (NEW in 3b), 3 row_vars x tvhours means:
  ANOVA=TRUE 0.220 s | ANOVA=FALSE 0.080 s | share 0.140 s (Welch + classic F, all tables one pass).

Also fixed: the pre-3b tab_pvalue_lines crashed ("Names must be unique") when a col_var name
overlapped a row_var name; the Phase 3b tidy-attribute rewrite handles it.
